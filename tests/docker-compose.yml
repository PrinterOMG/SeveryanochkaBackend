version: '3'

networks:
  backend:
    driver: bridge

services:
  postgres:
    image: postgres:16
    networks:
      - backend
    expose:
      - 5432
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=test
      - POSTGRES_PASSWORD=secret-password
      - POSTGRES_DB=test
